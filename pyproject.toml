[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week1_bacas"
version = "0.0.1"
dependencies = []

[project.scripts]
todo = "resolution_week1_bacas.main:main"